pipeline {
    agent any
    stages {
        stage('dockerrun') {
            steps {
                // Deploy the Docker container
                script {
                    sh 'docker run -d -p 8082:80 --name my-app-container nginx'
                }
            }
        }
    }
}
